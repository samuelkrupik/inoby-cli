{
  "name": "code-generator",
  "version": "1.0.0",
  "description": "Generates boilerplate code from template",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "rm -rf ./build/* && tsc -w"
  },
  "bin": {
    "code-generator": "./build/index.js"
  },
  "keywords": [],
  "author": "Samuel Krupik",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.23",
    "typescript": "^4.6.3"
  }
}
